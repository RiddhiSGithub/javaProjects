package inheritanceJavaOOP;

public class mainClass {
    //inheritance is the concept of creating a new class based on an existing class.
    //The derived or child class inherits all the properties and behaviors (variables, methods) from its parent or superclass.    
    public static void main(String[] args) {
        childclass child = new childclass();
        child.sum();
        child.mius();   
    }
}
