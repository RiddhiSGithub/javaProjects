public class operatorsJava {
    // Arithmatic Operators
    // 1. Binary:- +, -, *, /, % ;
    // 2. Uanary:- ++, -- ;
    // 2.1 Post-Increment
    // 1. use value 2.then increment
    //  a++ --> a+1
    //  a-- --> a-1
    // 2.2 Pre-Increment
    // 1. Increment value 2. use value
    //  ++a --> a+1
    //  --a --> a-1

    // Relational Operator
    // ==, !=, >, <, >=, <=

    // Logical Operator
    // &&, ||, !
    
    // decimal to binary of 3:
    // 2 | 6 0  ^
    // 2 | 3 1  |
    //   | 1    |
    //  3 == 110

    // binary to decimal 110:
    // 1        1     0 
    // <------------------
    // 1*2^2 + 1*2^1 + 0*2^0 == 6

    // memory address in java hexadecimal


    // Bitwise Operator:
    //  &(Binary AND), |, ^, ~, >>, <<
    // check word file bit Advance

    // Assignment Operator
    // =, +=, -=, *=, /=
    // a=b
    // a+=b --> a=a+b
    // a-=b --> a=a-b
    // a*=b --> a=a*b
    // a/=b --> a=a/b

}
